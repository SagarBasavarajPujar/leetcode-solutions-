class Solution(object):
    def smallerNumbersThanCurrent(self, nums):
        answers=list()
        for i in nums:
            count=0
            for j in nums:
                if j<i:                  
                    count+=1
            answers.append(count)
        return answers

